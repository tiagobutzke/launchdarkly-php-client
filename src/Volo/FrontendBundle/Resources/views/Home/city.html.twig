{% extends 'VoloFrontendBundle:Layout:page.html.twig' %}

{% block content %}
    <h1 class="text-center">{{ city.name|e }}</h1>
    <ul>
        {% for vendor in vendors %}
            <li>{{ vendor.name|e }}</li>
        {% endfor %}
    </ul>


{% endblock %}
